# dev-spec NN：<任务标题>

> **元信息**
> - 任务简述：
> - 创建会话 / 日期：
> - 分支：`internal-dev`
> - 对应 commit：
> - 依赖敏感点摘要：（一句话；无则填「无」）

---

## 一、需求背景

- 要解决什么问题 / 为什么改。
- 触发来源（用户需求 / bug / 重构）。

---

## 二、现状分析

- 涉及的现有代码、模块、接口（基于代码探查的关键事实）。
- 引用 `file_path:line` 便于定位。

---

## 三、方案设计

- 设计思路 + 取舍。
- 关键代码片段（核心结构，非全文）。

---

## 四、改动清单

- 逐文件列出：新增 / 修改 / 删除，附**关键片段**（散文 + 片段，非逐行 diff）。
- 例：
  - 新增 `spring-ai-harness-utils/.../storage/Foo.java`（附核心结构片段）
  - 修改 `.../tool/Bar.java`（改动点说明 + 片段）

---

## 五、依赖敏感点

- 涉及的外部依赖 / 版本 / 组件。
- 公司内可能差异 + 适配建议。
- 无则填「无」。

---

## 六、验证方式

- 单测（类名 + 覆盖点）/ 手动验证步骤。
- 预期结果。

---

## 七、复刻步骤

- 公司内按顺序执行的操作清单：定位文件 -> 应用改动 -> 适配依赖敏感点 -> 验证。
- 引用分支 commit 作为兜底对照（`git diff main..internal-dev -- <path>`）。
